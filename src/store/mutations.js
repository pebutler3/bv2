const mutations = {
  SET_ROUTES: (state, routes) => state.routes = routes,
  SET_BIOS: (state, bios) => state.bios = bios,
  SET_BOOKS: (state, books) => state.books = books,
};

export default mutations;
